import { ResourceServerErrorHandler } from './resource-server-error-handler';

describe('ResourceServerErrorHandler', () => {
  it('should create an instance', () => {
    expect(new ResourceServerErrorHandler()).toBeTruthy();
  });
});
